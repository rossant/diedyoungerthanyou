# Died Younger Than You

A mobile-first interactive timeline showing remarkable people who died younger than the visitor, and what they accomplished in the time they had.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The site is intentionally small: Vite + TypeScript + plain HTML/CSS, with a static curated dataset and no runtime API.
